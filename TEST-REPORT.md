# NCM Player 测试报告（v1.2.0）

> 测试时间：2026-08-20（自动化执行）
> 测试环境：Windows 10.0.26200 · Node.js v24.19.0 · Electron 43.4.1 · Chromium（Electron 内置）

---

## 一、测试概览

| 测试套件 | 用例数 | 通过 | 失败 | 结论 |
|----------|-------:|-----:|-----:|------|
| TypeScript 类型检查 | — | ✅ | 0 错误 | 通过 |
| 前端构建（Vite） | — | ✅ | 0 错误 | 通过 |
| 后端接口测试（API） | 13 | 13 | 0 | 通过 |
| 端到端 UI 测试（E2E） | 29 | 29 | 0 | 通过 |

**总体结论：全部通过 ✅（42/42）**

---

## 二、构建与类型检查

- `npm run typecheck` → ✅ 0 错误
- `npm run build` → ✅ 45 模块，`dist/assets/index-*.js 783.56 kB (gzip 212 kB)`

---

## 三、后端接口测试（`npm run test:api`）

✅ **通过 13/13**（搜索 / 歌曲详情 / 播放地址 / 歌词 / 扫码 key·create·check / 榜单 / 歌曲榜 / 歌单详情 / 登录状态 / 每日推荐 / 私人FM）。

---

## 四、端到端 UI 测试（`scripts/e2e-test.mjs`）

Playwright 驱动真实 Electron 应用，29 项全部通过：

### 4.1 无边框窗口 + 界面（7 项）
自定义标题栏、3 个窗口控制按钮、侧边栏、播放条、3D 画布（含辉光）、搜索框 ✅

### 4.2 主题系统（3 项）
浅色 / 深色 / 跟随系统 ✅

### 4.3 播放与歌词（6 项）
搜索返回 50 首、找到免费歌曲、audio.src 设置、音频播放中、歌词 100 行、逐字卡拉OK ✅

### 4.4 独立播放页（4 项，本轮新增）
| # | 用例 | 结果 |
|---|------|------|
| 17 | 播放页打开（点击展开按钮） | ✅ |
| 18 | 播放页-返回按钮 | ✅ |
| 19 | 播放页-歌词可见 | ✅ 100 行 |
| 20 | 播放页返回浏览页 | ✅ |

### 4.5 登录 / 3D / 队列 / 沉浸式（9 项）
已登录状态、6 种可视化模式切换（粒子/星系/隧道）、播放队列面板、沉浸式歌词、退出沉浸式、无渲染进程错误 ✅

✅ **通过 29/29**。截图存于 `test-results/`（含 `e2e-04b-nowplaying.png` 播放页截图）。

---

## 五、本轮修复的 Bug

| # | 问题 | 修复 |
|---|------|------|
| 1 | 歌词被右侧列表面板遮挡（`right` 定位重叠） | 歌词改到舞台左侧 `left: 20px`，不再与列表重叠 |
| 2 | 「上一首」无“3 秒内重播当前曲”逻辑 | `prev()` 在进度 >3s 时回到当前曲开头 |
| 3 | 无歌曲时点播放会置为 playing 但无声音 | `togglePlay()` 增加空态保护 |
| 4 | `normalizeSong` 封面回退逻辑失效（空串 `??` 陷阱） | 改用 `al?.picUrl || album?.picUrl || ''` |
| 5 | 私人 FM 播完最后一首会循环前 3 首而不换新 | 新增 `fmNext()`，播完自动拉取下一批 |
| 6 | FM 用 `fmIndex` 与队列 `index` 双轨易失同步 | 移除 `fmIndex`，统一用队列 `index` 追踪 |
| 7 | 播放页上点「队列」按钮无反应 | 队列按钮改为先切回浏览页再打开队列 |
| 8 | 主题启动时闪烁（先深色再变浅色） | `index.html` 内联脚本在 React 挂载前预加载主题 |
| 9 | 歌单卡片显示无意义的“简介”文案 + 无用导出 | 清理为仅显示曲目数 |
| 10 | 长歌名会顶到舞台中央的模式按钮 | `stage-title` 增加截断与最大宽度 |

---

## 六、打包验证（`npm run dist:win`）

| 文件 | 说明 |
|------|------|
| `NCM Player-Setup-1.2.0-x64.exe` | NSIS 安装包 |
| `NCM Player-Portable-1.2.0-x64.exe` | 免安装便携版 |

打包后的便携版已通过启动冒烟测试（退出码 0，截图正常）。

---

## 七、已知限制与说明

- 扫码确认需真机（应用内已存在有效登录会话，验证登录全链路有效）。
- VIP 歌曲需登录且为会员才能完整播放（版权限制）。
- 播放页切换时 3D 场景会重新初始化（约 0.2s），属预期行为。
