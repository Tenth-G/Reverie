# Reverie 测试报告（v1.0.0）

> 测试时间：2026-08-20（自动化执行）
> 测试环境：Windows 10.0.26200 · Node.js v24.19.0 · Electron 43.4.1 · Chromium（Electron 内置）

---

## 一、测试概览

| 测试套件              | 用例数 | 通过 |   失败 | 结论 |
| --------------------- | -----: | ---: | -----: | ---- |
| TypeScript 类型检查   |      — |   ✅ | 0 错误 | 通过 |
| 前端构建（Vite）      |      — |   ✅ | 0 错误 | 通过 |
| Electron 启动冒烟测试 |      1 |   ✅ |      0 | 通过 |
| 后端接口测试（API）   |     15 |   15 |      0 | 通过 |
| 端到端 UI 测试（E2E） |     26 |   26 |      0 | 通过 |

**总体结论：全部通过 ✅（41/41）**

---

## 二、构建与类型检查

- `npm run typecheck` → ✅ 0 错误
- `npm run build` → ✅ `dist/assets/index-*.js 232.25 kB (gzip 72.89 kB)`（移除 Three.js 后体积大幅下降）

---

## 三、后端接口测试（`npm run test:api`）

✅ **通过 15/15**（搜索 / 歌曲详情 / 播放地址 / 歌词 / 扫码 key·create·check / 榜单 / 歌曲榜 / 歌单详情 / 登录状态 / 每日推荐 / 私人FM / **红心 `/like`** / **喜欢列表 `/likelist`**）。

新增的 `/like` 与 `/likelist` 未登录时返回 `code=301`（需登录），符合预期。

---

## 四、端到端 UI 测试（`scripts/e2e-test.mjs`）

Playwright 驱动真实 Electron 应用，26 项全部通过：

### 4.1 标题栏 + 顶部导航（7 项）

窗口标题「Reverie」、标题栏居中显示「Reverie」、3 个窗口控制按钮、顶部导航栏、左侧导航已移除、悬浮播放栏、登录按钮仅显示「登录」二字 ✅

### 4.2 未登录状态（3 项）

未登录首页不显示数据（`login-empty`）、未登录首页无分区、未登录无头像下拉 ✅

### 4.3 主题（3 项）

浅色 / 深色 / 跟随系统 ✅

### 4.4 搜索下拉（3 项）

搜索胶囊展开、搜索下拉结果 12 条、搜索不跳转页面 ✅

### 4.5 播放与播放页（8 项）

| #                              | 用例                     | 结果 |
| ------------------------------ | ------------------------ | ---- |
| 排行榜点击播放（audio 播放中） | ✅                       |
| 播放页打开（整页）             | ✅                       |
| 播放页隐藏顶部导航             | ✅                       |
| **播放页封面图片显示**         | ✅（修复专辑图片不显示） |
| 播放页歌词在封面上方           | ✅ 100 行                |
| 无 3D 画布                     | ✅                       |
| 播放页返回                     | ✅                       |
| 无渲染进程错误                 | ✅                       |

✅ **通过 26/26**。截图存于 `test-results/`。

---

## 五、本轮改动说明

1. **更名**：播放器名 Reverie（中文名「白日梦」），标题栏居中显示英文名（默认只显示 Reverie）。
2. **未登录不显示数据**：未登录首页仅显示登录引导；登录按钮仅「登录」二字、无图标。
3. **持久化**：主题、音量、播放模式、歌词主题、字号、翻译均持久化到 localStorage。
4. **精简页面**：移除独立的每日推荐 / 推荐歌单 / 播放队列页面；导航精简为 首页 / 排行榜 / 私人FM / 我的歌单。
5. **搜索下拉**：点击搜索图标展开胶囊搜索框，结果以**下拉面板**呈现，不再跳转页面。
6. **透明播放栏**：播放栏改为高透毛玻璃（`--glass-player`）。
7. **整页播放页**：进入播放页隐藏顶部导航，整页显示「歌词在上 + 封面在下」。
8. **移除 3D 与沉浸歌词**：删除 Three.js 3D 可视化与沉浸式歌词，播放页改为纯 2D 封面（面朝屏幕）+ 上方歌词；**修复了封面图片不显示的问题**。
9. **更多接口**：新增红心 `/like`、喜欢列表 `/likelist`（播放栏红心按钮）。
10. **头像下拉**：登录后点击头像弹出下拉（会员信息 / 切换账号 / 退出登录），不再跳转设置。

---

## 六、打包验证（`npm run dist:win`）

| 文件                             | 说明         |
| -------------------------------- | ------------ |
| `Reverie-Setup-1.0.0-x64.exe`    | NSIS 安装包  |
| `Reverie-Portable-1.0.0-x64.exe` | 免安装便携版 |

---

## 七、已知限制与说明

- 扫码登录需真机操作，自动化环境无法代扫码，因此**登录态下的界面（头像下拉、会员信息、红心、首页数据）未能自动化覆盖**，其逻辑已实现并通过类型检查与编译。
- 未登录状态下，排行榜/搜索的免费歌曲可正常播放（已自动化验证）。
- VIP 歌曲需登录且为会员才能完整播放（版权限制）。
