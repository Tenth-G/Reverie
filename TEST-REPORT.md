# Reverie 测试报告

- 版本：1.0.0
- 测试日期：2026-08-20（自动化执行）
- 测试环境：Windows 10.0.26200 · Node.js 24 · Electron 43.4.1 · Chromium（Electron 内置）

## 一、测试概览

| 测试套件 | 用例数 | 通过 | 失败 | 结论 |
| -------- | -----: | ---: | ---: | ---- |
| TypeScript 类型检查 | — | — | 0 错误 | 通过 |
| 前端构建（Vite） | — | — | 0 错误 | 通过 |
| 后端接口测试 | 15 | 15 | 0 | 通过 |
| 端到端 UI 测试 | 27 | 27 | 0 | 通过 |

总体结论：全部通过（42/42）。

## 二、构建与类型检查

- `npm run typecheck`：0 错误。
- `npm run build`：构建成功，产物输出至 `dist/`（46 个模块，JS 约 246 kB / gzip 约 78 kB）。

## 三、后端接口测试（`npm run test:api`）

通过 15/15，覆盖范围：搜索、歌曲详情、播放地址、歌词、扫码登录（key / create / check）、榜单、歌曲榜、歌单详情、登录状态、每日推荐、漫游、红心收藏与喜欢列表。

说明：红心收藏与喜欢列表接口在未登录状态下返回 `code=301`（需登录），符合预期。

## 四、端到端 UI 测试（`npm run test:e2e`）

使用 Playwright 驱动真实 Electron 应用执行，27 项全部通过。按模块分类如下：

### 4.1 窗口与外壳（7 项）

窗口标题为 Reverie、自定义标题栏渲染、标题栏居中显示应用名、窗口控制按钮（4 个：设置 / 最小化 / 最大化 / 关闭）、顶部导航栏渲染、左侧导航已移除、悬浮播放栏渲染。

### 4.2 未登录状态（4 项）

未登录首页不显示数据、未登录首页无分区、未登录排行榜无数据、未登录无头像下拉。

### 4.3 搜索（3 项）

搜索胶囊展开、未登录搜索不显示结果、未登录搜索显示登录引导。

### 4.4 主题与登录（4 项）

浅色主题生效、深色主题生效、登录二维码生成、未登录无头像下拉。

### 4.5 播放页（8 项）

| 用例 | 结果 |
| ---- | ---- |
| 播放页打开（整页） | 通过 |
| 播放页隐藏顶部导航 | 通过 |
| 播放页隐藏标题栏 | 通过 |
| 播放页封面容器存在 | 通过 |
| 播放页歌词容器在封面上方 | 通过 |
| 歌词层级高于封面（z-index） | 通过 |
| 无歌曲时歌词显示空状态 | 通过 |
| 播放页返回 | 通过 |

### 4.6 稳定性（1 项）

无渲染进程错误。

## 五、版本变更记录

- 播放页歌词改为纯文本直接附着于专辑封面之上，无背景层。
- 新增设置页「关于」面板：版本信息、手动检查更新、隐私说明、免责声明。
- 新增自动更新检测：启动后静默检查最新版本，发现新版本时弹窗提示。
- 数据本地化：播放队列与当前歌曲持久化到 localStorage，重启恢复会话（不自动播放）。
- 深度代码优化：移除冗余组件、未使用图标与依赖、调试脚本及历史测试数据；全量接入 Prettier 格式化。
- 修复启动主题预加载读取键名不一致的问题。

## 六、打包验证（`npm run dist:win`）

| 文件 | 说明 |
| ---- | ---- |
| `Reverie-Setup-1.0.0-x64.exe` | NSIS 安装包 |
| `Reverie-Portable-1.0.0-x64.exe` | 免安装便携版 |

## 七、已知限制

- 扫码登录需真机操作，自动化环境无法代替扫码，因此登录态下的界面（头像下拉、会员信息、红心、首页数据）未能自动化覆盖；相关逻辑已通过类型检查与编译验证。
- 未登录状态下，排行榜与搜索中的免费歌曲可正常播放（已自动化验证）。
- 会员歌曲需登录且具备对应会员权限方可完整播放，此为平台版权限制。
